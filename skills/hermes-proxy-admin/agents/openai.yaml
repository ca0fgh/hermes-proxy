interface:
  display_name: "Hermes-Proxy Admin"
  short_description: "Manage Hermes-Proxy admin accounts and settings"
  default_prompt: "Use $hermes-proxy-admin to inspect and manage Hermes-Proxy backend accounts safely."

policy:
  allow_implicit_invocation: true
